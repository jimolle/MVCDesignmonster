@model MVCDesignmonster.WebUI.ViewModels.StartPageViewModel

@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>@Model.WelcomeTitle</h1>
    <p class="lead">@Model.WelcomeText</p>

    <p>@Html.ActionLink("Se min profil!", "Index", "Profile", null, new { @class = "btn btn-success btn-lg" })</p>
</div>

<div class="row">
    <div class="col-md-6">
        <h2>Testing MinWords attribute</h2>
        <h3>...</h3>

    </div>
    <div class="col-md-6">
        <h2>BUGS</h2>
        <ul>
            <li class="alert alert-danger">Verkar som om databaskopplingen inte alltid stängs ordentligt, blir ibland problem om man gör en ny build utan att manuellt stänga databaskopplingen i VS...</li>
            <li class="alert alert-danger">Med nuvarande repos så öppnar den en separat DbContext för varje repo jag instansierar! Här hade det ju varit något med UoW-pattern...</li>
            <li class="alert alert-danger">Man hade kunnat sätta upp modellerna med relationer till varandra, hade nog underlättat, men provade utan för att se fördelar/nackdelar med det.</li>
            <li class="alert alert-danger">Ibland blir filloggern överhettad och kastar exception (används av en annan process, kanske dispose problem?).</li>
            <li class="alert alert-success">ActiveUserService Seems to be OK. OBS! Timout session & cookie = 1 ATM.</li>
        </ul>
    </div>

</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/App/CustomValidators.js"></script>
}